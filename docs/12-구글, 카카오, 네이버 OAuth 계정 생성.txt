[구글]
1. https://console.cloud.google.com 접속
2. "새 프로젝트" 생성
3. 생성된 프로젝트 선택
4. "API 및 서비스" 선택
5. "OAuth 동의 화면" 메뉴 선택
    - 정보들 입력
6. 5번 과정 완료 후 좌측 메뉴에서 개요를 선택하고 "OAuth Client ID 만들기" 버튼 클릭
    애플리케이션 유형: 웹 애플리케이션
    이름: My Diary
    승인된 리디렉션 URI: http://localhost:3000/oauth/google/redirect
    만들기 버튼

    만들기가 완료되면 생성된 클라이언트의 상세 정보 페이지가 표시되는데 "클라이언트 보안 비밀번호" 영역에서
    JSON 파일을 다운로드 받을 수 있고 파일 내의 client_id, client_secret이 해당 앱의 고유 식별자가 된다.
    JSON 파일은 최초에만 다운로드 받을 수 있고 분실할 경우 새로운 secret을 추가해야만 다운로드 받을 수 있다.

    생성된 OAuth Client ID의 상세 정보는 좌측 메뉴의 "클라이언트"에서 다시 확인할 수 있다.
7. 구글로부터 제공받을 사용자 정보 범위 결정은 좌측 메뉴의 "데이터 액세스"에서 할 수 있다.
    "범위 추가 또는 삭제" 버튼 클릭하면 필요 항목을 설정 가능하다.
    email, profile, openid 는 기본적으로 제공되는 정보이다.
    나머지 추가 정보를 받아오기 위해서는 "대상" 메뉴에서 "앱 개시"를 하거나 "테스트 사용자"에 사용자ID를 지정해줘야만
    사용이 가능하다.

    "앱 개시"를 하게 될 경우 구글의 승인 절차가 필요하다.

8. 인증 흐름
    (1) 구글 로그인 버튼 클릭 시 구글 계정 OAuth URL로 리다이렉션 및 구글 로그인
            다운로드 받은 secret.json에서 다음 항목을 작성 후 웹페이지 이동

            googleAuthUri: "https://accounts.google.com/o/oauth2/v2/auth" << 강의 URL 이후 버전업 되어 변경됨
            googleClientId: client_id
            googleRedirectUri: redirect_uris 배열 내용 중 1건,
            googleScope: "openid profile email",
            googleResponseType: "code"

            const authUri = `${this.googleAuthUri}?client_id=${this.googleClientId}&redirect_uri=${this.googleRedirectUri}&response_type=${this.googleResponseType}&scope=${this.googleScope}`;

            window.location.href = authUri;
    (2) 구글 로그인이 정상적으로 이루어지면 (1)에서 전달한 redirect_uri 경로로 인가코드를 리턴해 줌
        프론트엔드 단에서 redirect_uri에 대응하는 웹페이지를 구현해야 함.

        [GoogleRedirect.vue]
        <template>
            <div>
                구글 로그인 진행 중...
            </div>
        </template>

        <script>
        import axios from 'axios';

        export default {
            created() {
                const code = new URL(window.location.href).searchParams.get("code");
                this.sendCodeToServer(code);
            },
            methods: {
                async sendCodeToServer(code) {
                    const response = await axios.post("http://localhost:8080/member/google/login", {code: code});
                    const token = response.data.token;
                    localStorage.setItem("token", token);
                    window.location.href = "/";
                }
            }
        }
        </script>

    (3) 클라이언트에서는 리턴받은 인가코드를 서버로 전송하고 서버는 인가 코드로 구글 OAuth 2.0 액세스 토큰을 가져온다.
            다운로드 받은 secret.json에서 다음 항목을 작성 후 웹페이지 이동

            [서버단 구현]
            googleTokenUri: token_uri
            googleAuthCode: (2)번 응답 값
            googleClientId: client_id
            googleClientSecret: client_secret
            googleGrantType:
            googleRedirectUri: redirect_uris 배열 내용 중 1건 << 실질적으로 콜백되지는 않음

            const getTokenUri = `${this.googleTokenUri}?code=${this.googleAuthCode}&client_id=${this.googleClientId}&client_secret=${this.googleClientSecret}&grant_type=${this.googleGrantType}&redirect_uri=${this.googleTokenRedirectUri}`;

            const response = await axios.post("http://localhost:8080/member/google/login", {code: code});
            const token = response.data.token;
            localStorage.setItem("token", token);

    (4) 발급받은 액세스 토큰을 사용해 Header에 Authorization값 세팅하고 사용자 정보를 조회한다.
            https://openidconnect.googleapis.com/v1/userinf << 강의 당시 가장 최신으로 보인다고 함
            https://www.googleapis.com/userinfo/v2/me
            https://www.googleapis.com/oauth2/v3/userinfo



    (5) 조회된 사용자 정보로 로그인 처리, 회원 가입 등의 정해진 철자에 맡게 처리한다.

[카카오]
1. https://developers.kakao.com/console/app 접속
2. "앱 생성" 클릭
    이름: 단어장
    회사명: 개인
    카테고리: 교육
    저장 버튼
3. 생성된 프로젝트 선택
4. 좌측 메뉴 앱 > "플랫폼 키" 선택: REST API 키가 client_id
5. 좌측 메뉴 카카오 로그인 > 동의항목
    (1) 개인정보 항목을 보면 이메일에 대한 권한이 없으므로 "추가 기능 설정" 을 해줘야 함
    (2) "추가 기능 설정" 클릭
        - "비즈 앱 등록" 클릭
        - 앱 기본 정보 > 앱 아이콘 등록 (*)
        - 비즈니스 정보 > "카카오비즈니스 통합 서비스 약관 동의" > "카카오 인증" > "약관동의"
        - 다시 비즈니스 정보 > "개인 개발자 비즈 앱 전환" > 개인 개발자 비즈 앱 전환 목적 (이메일 필수 동의)
        - 테스트 앱 > "테스트 앱 생성" 클릭 > 테스트 앱 이름 입력(단어장-DEV)
    (3) (2) 완료 후 개인정보 항목을 보면 이메일에 대한 권한이 생긴 것을 확인할 수 있음
    (4) 개인정보 설정
        - 닉네임: 선택 동의
        - 프로필 사진: 선택 동의
        - 카카오계정(이메일): 필수 동의
    (5) 키 조합
        client_id: REST API 키
        client_secret: REST API 상세 화면 하단에 클라이언트 시크릿 항목에서 위치하는 카카오 로그인 코드
6. 좌측 메뉴 카카오 로그인 > 일반
    - 사용 설정: 활성화
    - OpenID Connect: 활성화
7. 리다이렉션 URI 설정
    앱 > 플랫폼 키 > REST API 선택 > 카카오 로그인 리다이렉트 URI에 입력
8. 인증 흐름 (공식 문서에 잘 정리되어있으니 참조): https://developers.kakao.com/docs/ko/kakaologin/rest-api#request-code-info
    kakaoAuthUri: "https://kauth.kakao.com/oauth/authorize"
    kakaoClientId: client_id (REST API 키)
    kakaoRedirectUri: redirect_uri (REST API 상세에서 설정한 경로)
    kakaoResponseType: "code"

    const authUri = `${this.kakaoAuthUri}?client_id=${this.kakaoClientId}&redirect_uri=${this.kakaoRedirectUri}&response_type=${this.googleResponseType}`;

    window.location.href = authUri;

9. 공식문서
    카카오 로그인: https://developers.kakao.com/docs/ko/kakaologin/rest-api

10. 사전조건 확인
    공식문서에서 사용하고자하는 API 페이지에 가보면 어떤 조건이 충족되어야 하는지 설명 해준다.

    예) 카카오 로그인 - 인가 코드 요청 API의 사전 설정
        REST API 키
        카카오 로그인 활성화
        리다이렉트 URI
        동의항목
        OpenID Connect 활성화(선택)
        간편가입(선택)